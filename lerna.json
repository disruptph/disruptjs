{
  "npmClient": "yarn",
  "useWorkspaces": true,
  "version": "independent",
  "packages": [
    "packages/*"
  ],
  "command": {
    "publish": {
      "ignoreChanges": ["*.md"]
    },
    "version": {
      "allowBranch": ["master"],
      "message": "chore(release): publish"
    }
  }
}
