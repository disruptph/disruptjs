{
  "name": "disruptjs",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.0",
    "typescript": "^3.9.3"
  },
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap && yarn run build",
    "publish:dryrun": "lerna version --conventional-commits --yes --no-git-tag-version --no-push --allowBranch",
    "publish": "lerna publish from-git --conventional-commits --yes"
  }
}
