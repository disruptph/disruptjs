{
  "name": "disruptjs",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.0",
    "typescript": "^3.9.3"
  },
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap && yarn run build",
    "version": "lerna version",
    "version:dryrun": "yarn run version --no-git-tag-version --no-push",
    "publish": "lerna publish from-git"
  }
}
