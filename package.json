{
  "name": "disruptjs",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.0",
    "typescript": "^3.9.3"
  },
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap && yarn build",
    "prepublish": "yarn bootstrap && lerna version",
    "prepublish:dryrun": "yarn prepublish --no-git-tag-version --no-push",
    "publish": "lerna publish from-git"
  }
}
